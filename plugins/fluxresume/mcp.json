{
  "inputs": [
    {
      "id": "fluxresume_api_key",
      "type": "promptString",
      "description": "Your FluxResume MCP API key — generate one in FluxResume → Settings → AI Agent",
      "password": true
    }
  ],
  "mcpServers": {
    "fluxresume": {
      "url": "http://localhost:5000/mcp",
      "headers": {
        "Authorization": "Bearer ${input:fluxresume_api_key}"
      }
    }
  }
}
